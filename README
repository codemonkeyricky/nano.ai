This project includes minimalistic (yet functional) implementations of AI
inference engines. Each engine is contained in a single file, written in a few
hundred lines of pure C without any external package dependencies.